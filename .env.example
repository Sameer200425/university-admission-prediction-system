# Docker Compose / local configuration
# Copy to .env (which is gitignored) and adjust.

# Backend Basic Auth (FastAPI)
API_AUTH_USERNAME=admin
API_AUTH_PASSWORD=password

# Frontend build-time configuration (Vite)
# In docker-compose, the frontend talks to the backend through the Nginx /api proxy.
VITE_API_BASE_URL=/api
